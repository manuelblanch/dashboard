<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="androidhive.dashboard"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk android:minSdkVersion="8" />

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name" >
        <activity
            android:label="@string/app_name"
            android:name="com.iesebre.DAM2.dashboard.AndroidDashboardDesignActivity" >
            <intent-filter >
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- News Feed Activity -->
        <activity android:name="com.iesebre.DAM2.dashboard.NewsFeedActivity" ></activity>

        <!-- Friends Activity -->
        <activity android:name="com.iesebre.DAM2.dashboard.FriendsActivity" ></activity>

        <!-- Messages Activity -->
        <activity android:name="com.iesebre.DAM2.dashboard.MessagesActivity" ></activity>

        <!-- Places Activity -->
        <activity android:name="com.iesebre.DAM2.dashboard.PlacesActivity" ></activity>

        <!-- Events Activity -->
        <activity android:name="com.iesebre.DAM2.dashboard.EventsActivity" ></activity>
        
        <!-- Photos Activity -->
        <activity android:name="com.iesebre.DAM2.dashboard.PhotosActivity" ></activity>
    </application>

</manifest>